data_ingestion:
  test_size: 0.20

feature_engineering:
  max_features: 10000
  ngram_range: [1, 3]

model_building:
  optuna_trials: 1
  mlflow_experiment: "dvc_pipeline"
  run_name: "LightGBM_Final"

model_register:
  mlflow_experiment: "dvc_pipeline"
  model_name: youtube_chrome_plugin_model
  target_stage: Staging
